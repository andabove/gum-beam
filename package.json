{
  "name": "gum-beam-root",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter=@andabove/gum-beam run build",
    "dev": "pnpm -r --parallel run dev",
    "typecheck": "pnpm -r run typecheck",
    "fix": "pnpm -r run fix",
    "warm": "pnpm -r run prepare"
  },
  "devDependencies": {
    "oxlint": "latest",
    "oxfmt": "latest"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@parcel/watcher", "esbuild", "vue-demi"]
  }
}
